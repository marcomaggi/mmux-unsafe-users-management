# /etc/sudoers.d/mmux-unsafe-users-management.conf --
#
# Notes:
#
# * Do *not* create a host alias like the following, because it will
#   cause command specifications not to work (even though "sudo" does
#   not reject it):
#
#      Host_Alias LOCALHOST = localhost
#
#   To specify "localhost" as hostname, just use "ALL".
#

# User alias specification
User_Alias	MMUX_USERS		= %users

# Command aliases
Cmnd_Alias	MMUX_UNSAFE_USERS_ADD	 = /usr/sbin/mmux-unsafe-users sudo-add [a-zA-Z][a-zA-Z0-9-]*
Cmnd_Alias	MMUX_UNSAFE_USERS_DEL	 = /usr/sbin/mmux-unsafe-users sudo-del [a-zA-Z][a-zA-Z0-9-]*
Cmnd_Alias	MMUX_UNSAFE_USERS_BIND	 = /usr/sbin/mmux-unsafe-users sudo-bind [a-zA-Z][a-zA-Z0-9-]*
Cmnd_Alias	MMUX_UNSAFE_USERS_UNBIND = /usr/sbin/mmux-unsafe-users sudo-unbind [a-zA-Z][a-zA-Z0-9-]*
Cmnd_Alias	MMUX_UNSAFE_USERS_CMDS	 = MMUX_UNSAFE_USERS_ADD, MMUX_UNSAFE_USERS_DEL, MMUX_UNSAFE_USERS_BIND, MMUX_UNSAFE_USERS_UNBIND

# User privilege specification
MMUX_USERS	ALL = MMUX_UNSAFE_USERS_CMDS

### end of file
# Local Variables:
# mode: script
# End:
